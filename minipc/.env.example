# Copy to `.env` (same folder as compose.jarvis-agent.yml) and fill in.
# This file holds a secret — keep it on the minipc, never commit the real .env.

# Supabase service_role key for the ClearRoute project.
# Find it: Supabase dashboard -> Project Settings -> API -> Project API keys ->
# "service_role" (secret). It lets the worker read/write the job queue.
SUPABASE_SERVICE_ROLE_KEY=paste-your-service-role-key-here
